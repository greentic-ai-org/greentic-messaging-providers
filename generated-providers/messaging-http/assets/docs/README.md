# Messaging HTTP

Initial answer-owned HTTP provider scope:

- inbound JSON `POST` webhook mapping
- optional bearer token and API key header validation
- outbound `GET`/`POST` request mapping
- URL templates using `{payload.field}` placeholders
