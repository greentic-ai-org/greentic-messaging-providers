{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Webex Provider-Core Config",
  "type": "object",
  "properties": {
    "default_room_id": {
      "type": "string",
      "minLength": 1,
      "description": "Webex room ID that the bot can read and post to."
    },
    "default_to_person_email": {
      "type": "string",
      "description": "Optional person email destination for direct messages."
    },
    "api_base_url": {
      "type": "string",
      "description": "Webex API base URL.",
      "default": "https://webexapis.com/v1"
    },
    "public_base_url": {
      "type": "string",
      "description": "Public base URL used to build inbound webhook callback URLs."
    },
    "webhook_secret": {
      "type": "string",
      "minLength": 1,
      "description": "Shared secret Webex uses to sign webhook requests with X-Spark-Signature."
    }
  },
  "required": [
    "public_base_url",
    "default_room_id",
    "webhook_secret"
  ],
  "additionalProperties": false
}
