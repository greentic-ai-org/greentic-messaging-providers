{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Webex Provider-Core Config",
  "type": "object",
  "properties": {
    "default_room_id": {
      "type": "string",
      "minLength": 1,
      "description": "Optional default Webex room ID used only for proactive outbound messages."
    },
    "default_to_person_email": {
      "type": "string",
      "description": "Optional default person email destination used only for proactive outbound messages."
    },
    "api_base_url": {
      "type": "string",
      "description": "Webex API base URL.",
      "default": "https://webexapis.com/v1"
    },
    "public_base_url": {
      "type": "string",
      "description": "Public base URL used to build inbound webhook callback URLs."
    },
    "webhook_secret": {
      "type": "string",
      "minLength": 1,
      "description": "Legacy config field. New setup writes WEBEX_WEBHOOK_SECRET to the secrets store."
    }
  },
  "required": [],
  "additionalProperties": false
}
