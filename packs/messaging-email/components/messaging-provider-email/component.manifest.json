{
  "id": "messaging-provider-email",
  "name": "messaging-provider-email",
  "version": "0.5.1",
  "world": "greentic:component/component-v0-v6-v0@0.6.1",
  "description": "Provider-core SMTP email sender (simulated).",
  "profiles": {
    "default": "default",
    "supported": [
      "default"
    ]
  },
  "config_schema": {
    "provider_config": {
      "format": "json",
      "path": "schemas/messaging/email/public.config.schema.json"
    }
  },
  "secret_requirements": [
    {
      "name": "GMAIL_CLIENT_SECRET",
      "scope": "tenant",
      "description": "Gmail OAuth client secret used to authenticate token refresh requests."
    },
    {
      "name": "GMAIL_REFRESH_TOKEN",
      "scope": "tenant",
      "description": "Gmail OAuth refresh token used to mint access tokens for the Gmail API."
    },
    {
      "name": "GMAIL_PUBSUB_VERIFICATION_TOKEN",
      "scope": "tenant",
      "description": "Shared token used to verify inbound Gmail Pub/Sub push notifications."
    }
  ],
  "operations": [
    {
      "name": "qa-spec",
      "input_schema": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "default",
              "setup",
              "upgrade",
              "remove"
            ]
          }
        },
        "required": [
          "mode"
        ],
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "description": "CBOR-encoded QA specification",
        "additionalProperties": true,
        "properties": {
          "questions": {
            "type": "array",
            "description": "List of questions in the QA spec"
          }
        }
      }
    },
    {
      "name": "apply-answers",
      "input_schema": {
        "type": "object",
        "properties": {
          "mode": {
            "type": "string",
            "enum": [
              "default",
              "setup",
              "upgrade",
              "remove"
            ]
          },
          "answers_cbor": {
            "type": "array",
            "items": {
              "type": "integer"
            },
            "description": "CBOR-encoded answers"
          }
        },
        "required": [
          "mode",
          "answers_cbor"
        ],
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "description": "CBOR-encoded result with config and optional cleanup plan",
        "additionalProperties": true,
        "properties": {
          "config": {
            "type": "object",
            "description": "Provider configuration"
          }
        }
      }
    }
  ],
  "capabilities": {
    "wasi": {},
    "host": {}
  }
}
