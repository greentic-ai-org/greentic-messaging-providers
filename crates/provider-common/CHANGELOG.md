# Changelog

## Unreleased

- Defined `greentic-messaging-provider-common` as the publishable shared provider crate package while preserving the Rust crate name `provider_common`.
- Added crates.io package metadata and documentation for the shared provider API boundary.
- Documented provider migration guidance for replacing duplicated provider helpers with shared APIs.
