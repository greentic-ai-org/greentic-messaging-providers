package greentic:state@1.0.0;

interface store {
    record state-error { message: string }

    get: func(key: string) -> result<option<list<u8>>, state-error>;
    set: func(key: string, value: list<u8>) -> result<(), state-error>;
    delete: func(key: string) -> result<(), state-error>;
}
